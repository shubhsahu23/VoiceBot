# Copy this file to `.env` and add your real credentials locally.
# DO NOT commit `.env` with real secrets to git.

# MongoDB connection URI (SRV):
# Format: mongodb+srv://<username>:<password>@<your-cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://shubh:********@clusterv.3hcny5d.mongodb.net/your_db_name

# Optional: specific DB name to use
MONGO_DB_NAME=smart_battery_db
